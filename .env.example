# ===========================================
# ReactFlux AI Docker Configuration
# ===========================================
# Copy this file to .env and modify as needed

# -------------------------------------------
# AI 后端配置
# -------------------------------------------
# AI 后端服务端口
AI_BACKEND_PORT=3001

# SQLite 数据库路径（容器内路径，一般不需要修改）
AI_DB_PATH=/app/data/reactflux.db

# -------------------------------------------
# 前端配置
# -------------------------------------------
# ReactFlux 前端端口
FRONTEND_PORT=2000

# -------------------------------------------
# Miniflux 配置（可选，用于自动登录）
# -------------------------------------------
# 如果你使用 Miniflux 作为 RSS 后端，可以配置以下选项
# 启用后，用户可以使用 Miniflux 账户自动登录

# MINIFLUX_URL=https://your-miniflux-server.com
# MINIFLUX_API_KEY=your_api_key

# -------------------------------------------
# 高级配置
# -------------------------------------------
# Node.js 环境（production/development）
NODE_ENV=production

# CORS 允许的源（* 表示允许所有）
CORS_ORIGIN=*
