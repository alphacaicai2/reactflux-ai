:2000

root * /srv

# API 代理 - 优先匹配
handle /api/ai* {
    reverse_proxy reactflux-ai-backend:3001
}

handle /api/digests* {
    reverse_proxy reactflux-ai-backend:3001
}

# 静态文件和 SPA 路由
handle {
    try_files {path} {path}/ /index.html
    file_server
}
